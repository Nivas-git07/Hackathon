# For a Vercel frontend used on the same computer as the local backend:
VITE_API_URL=http://127.0.0.1:8000

# For a public production backend, replace the value with its HTTPS URL:
# VITE_API_URL=https://api.your-domain.example
